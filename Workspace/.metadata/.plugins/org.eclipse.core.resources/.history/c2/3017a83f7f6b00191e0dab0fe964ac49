package buttonControls;

public class InputParser {
	String input;
	String output;
	
	public InputParser() {
		this("");
	}
	public InputParser(String input) {
		this.input = input;
		this.output = parse();
	}
	
	//Note: PE_MD_AS, separate in opposite order.
	public String parse() {
		String[][] AS = parsePlusMinus();
		
	}
	
	private String[][] parsePlusMinus() {
		String[] toAdd = input.split("+");
		
		return toSubtract(toAdd);
	}
	
	private String[][] toSubtract(String[] toAdd){
		
		return new String[0][];
	}

}
